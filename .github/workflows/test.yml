name: Test Runner

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Show system information
        run: |
          echo "Hello from GitHub Actions!"
          echo ""
          echo "CPU:"
          nproc
          echo ""
          echo "RAM:"
          free -h
          echo ""
          echo "Disk:"
          df -h
